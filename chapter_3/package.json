{
  "name": "chapter_3",
  "version": "1.0.0",
  "description": "A full stack todo app with NodeJS, SQLite and JWT Authentication",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon -L --watch ./src --ext js,json,mjs --exec \"node --env-file=.env\" ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Diego Moreno",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
